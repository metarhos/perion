apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-world-node.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "hello-world-node.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
